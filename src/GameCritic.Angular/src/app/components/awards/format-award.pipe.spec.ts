import { FormatAwardPipe } from './format-award.pipe';

describe('FormatAwardPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatAwardPipe();
    expect(pipe).toBeTruthy();
  });
});
